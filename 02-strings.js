// Strings // работа со строками

/* const name = 'Ildar';
const age = '11'; // число в ковычках - это строчка
const output = `
Hello
World
!!!
`; // обратные ковычки позволяют переносить текст в новую строку

console.log('Hello, my name is ' + name + ' and my age is ' + age); // старый вариант

console.log(`Hello, my name is ${name} and my age is ${age}`); // новый вариант

function getAge() {
    return age
}

console.log(`Hello, my name is ${name} and my age is ${getAge}`); // просто передал ссылочку на функцию

console.log(`Hello, my name is ${name} and my age is ${getAge.toString()}`); // вот как видит это программа

console.log(`Hello, my name is ${name} and my age is ${getAge()}`); // после добавления скобок вывел результат выполнения функции

console.log(`Hello, my name is ${name} and my age is ${getAge()}, which means i ${getAge() > 18 ? 'can' : 'can\'t'} drive`); // можно так

console.log(`Hello, my name is ${name} and my age is ${getAge()}, which means i ${getAge() > 18 ? 'can' : "can't"} drive`); // можно так

console.log(`Hello, my name is ${name} and my age is ${getAge()}, which means i ${getAge() > 18 ? 'can' : `can't`} drive`); // можно так */

/* const firstName = 'Ildar';

console.log(firstName.length);

console.log(firstName.toUpperCase());

console.log(firstName.toLowerCase());

console.log(firstName);

console.log(firstName.charAt(2));

console.log(firstName.charAt(0));

console.log(firstName.indexOf('d'));

console.log(firstName.indexOf('t'));

console.log(firstName.startsWith('ild'));

console.log(firstName.startsWith('Ild'));

console.log(firstName.toLowerCase().startsWith('ild'));

console.log(firstName.endsWith('dar'));

console.log(firstName.repeat(3));

const password = '     my super pass     ';

const password2 = 'normal pass';

console.log(password);

console.log(password.trim());

console.log(password2.padStart(10)); */

